
<%= form_with(model: booking, local: true) do |form| %>
  <% if booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(booking.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
      <% booking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= form.fields_for :passengers do |passengers| %>
  
    <%= passengers.text_field :name, placeholder: "Name", required: true, class: "input" %>
    <%= passengers.email_field :email, placeholder: "Email", required: true, class: "input" %>
  <% end %>

  <%= form.hidden_field :flight_id, value: params[:flight_id] %>  

  <%= form.submit %>
<% end %>
